{
  "name": "test-poject",
  "version": "0.0.1",
  "description": "An example project showing Cypress 10 using the oracledb plugin.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
  },
  "author": "",
  "license": "",
  "dependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^12.0.0",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.1.3",
    "cypress": "^10.4.0",
    "oracledb": "^5.4.0",
    "typescript": "^4.7.4"
  },
  "cypress-cucumber-preprocessor": {
    "json": {
      "enabled": true
    }
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
